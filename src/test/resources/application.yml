spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/techrag
    username: techrag
    password: techrag123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: localhost
      port: 6379

openai:
  api-key: test-api-key-for-testing
  model:
    embedding: text-embedding-3-small
    chat: gpt-5-mini

chunk:
  size: 500
  overlap: 50
